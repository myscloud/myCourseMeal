var CommentBox = React.createClass({
  render: function() {
    return (
      <div>
        This is an CommentBox.
      </div>
    );
  }
});

ReactDOM.render(
  <CommentBox />,
  document.getElementById('main')
);
